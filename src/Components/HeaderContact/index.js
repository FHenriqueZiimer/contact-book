import React from 'react';
import * as S from './styles';

const HeaderContact = ({ children }) => {
  return(    
    <S.ContactHeader>{children}</S.ContactHeader>
  )
}

export default HeaderContact;